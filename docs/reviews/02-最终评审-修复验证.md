核对完毕。以下为最终评审结论。

---

# 独立复审报告（commit 2cfe519，HEAD 即该提交，工作区仅 README 有未提交文档改动）

## 一、P1 逐项核对（6/6 通过，均经代码 + 行为/数据实证）

| 项 | 结论 | 依据 |
|---|---|---|
| **P1-1 分页口径** | **通过** | `db/repository.py:102-121` list_jobs 改为同 WHERE 条件 `COUNT(*)` + 元组返回；`app/main.py:25-32` 适配并用过滤后 total 算 pages、传 `total` 给模板。全项目 grep 仅 app/main.py 一个调用方，无遗漏。实测央国企筛选分页正确的记录与代码一致 |
| **P1-2 SearchAgent 接线** | **通过** | `pipeline.py:10,43-51` 在 `search_enabled && llm_enabled` 时执行（`config.py:46-52` 两属性均存在）；`search_agent.py:36-39` 轮转取样经索引推演：3 模板 × 5 方向，max_queries=10 覆盖全部模板，不再截断。README（未提交部分）补充了双 Key 条件说明 |
| **P1-3 分类覆盖与缓存** | **通过** | `repository.py:24-35` ON CONFLICT 用 `CASE WHEN excluded.confidence >= confidence` 守卫 + `MAX()` 保底，**行为实测**：高置信(0.7)不被低置信(0.3)覆盖、低→高正确升级；`pipeline.py:93-101` 缓存阈值 0.5。**name.strip() 一致性确认**：get 用 `name.strip()`（repository.py:42）、upsert 写入 `name.strip()`（repository.py:34），pipeline 传入的 company 已 strip，三者一致（实测带空格写入后查询命中）。DB 实证：423 家企业、24 家 list@1.0，无空名 |
| **P1-4 公司页负缓存** | **通过** | `nowcoder.py:100-101` 仅 `if name:` 才写缓存，失败同轮/下轮均重试；回归数据 37 条丢弃与该策略吻合 |
| **P1-5 LLM 缺字段** | **通过** | 双层兜底：`extractor.py:71-75` 解析后补齐 7 字段为字符串；`pipeline.py:16-17,110-111` REQUIRED_FIELDS setdefault 覆盖 insert_job 全部 9 个 job 字段。实测 sqlite3 命名参数容忍 dict 多余键（deadline 不炸） |
| **P1-6 日志路径** | **通过** | `main.py:15-21` `PROJECT_ROOT/logs` + `mkdir(parents=True, exist_ok=True)` + 绝对路径 FileHandler，import 期自建 |

## 二、重点回归项

- **pipeline run_id 生命周期**：run_id 在采集后、处理前创建（pipeline.py:55），finish_run 于独立连接收尾（71-72）；采集阶段失败不再产生 running 僵尸行；崩溃时同事务回滚（raw_items 一并丢失，见新问题 1）。count_jobs 的 last_run 排除 running，无展示问题。**通过**
- **errors 列表**：源失败、搜索失败、缺字段丢弃（pipeline.py:90）、单条异常四路均入列，detail 取前 20。仅抽取失败路径（81-83）只计数不入列（沿袭旧状，轻微）。**通过**
- **_escape_like 与 ESCAPE**：raw string 写法正确（SQL 单引号字符串中 `\` 即反斜杠，无 Python 转义冲突）；转义顺序先 `\` 后 `%`/`_` 正确。**实测**：`50%折` 只命中字面量、`含_下划线` 字面量命中、`x_y` 不跨条匹配。**通过**
- **iguopin**：已走 `BaseSource.post`（带重试/限速，iguopin.py:61）；终止条件 `if total and page*page_size >= total`（47-50 行）——total 缺失时不再误判“已到末页”，由空页/页数上限兜底，静默丢数据问题消除；seen_ids 跨关键词共享。**通过**
- **nowcoder**：搜索走 self.post（80 行）；单条 try/except（64-68）；recruitType 1/3 双维采集（README 社招 411 条为证）；`_ts_to_date` 北京时区 + int 强转 + 三类异常兜底。**通过**
- **app/main.py 与模板**：total 为过滤后口径；keyword 已 `| urlencode`。category/recruit_type 未 urlencode（见新问题 8）。**基本通过**
- **main.py**：日志目录已修；APScheduler `misfire_grace_time=3600, coalesce=True` 已加。**通过**
- **WAL/时区**：实测 `PRAGMA journal_mode` = wal；busy_timeout 仅在 init_db 连接上设置，但 `sqlite3.connect(timeout=15)` 已为所有连接提供同等 busy timeout，无功能缺口；now_str 全库北京时间，count_jobs “今日” 口径一致。**通过**

## 三、只读检查结果

- `python -m py_compile` 16 个源文件：**全部通过**
- `python tests/test_dedup.py`：**3/4 通过，test_classifier_lists 失败** — `ModuleNotFoundError: No module named 'openai'`（经 classifier 传递依赖，即原 P2-23，未修；openai 在 requirements.txt 中，装全依赖的环境可通过）。另注意当前解释器缺 fastapi/apscheduler，本机无法直接起 Web/调度——属环境问题非代码问题，交付时需 `pip install -r requirements.txt`

## 四、本轮修复新引入/遗留问题（无新 P1）

**P2：**
1. `pipeline.py:54-66` — 整轮处理在单连接单写事务内进行，LLM 抽取/分类的网络调用发生在事务开启之后；配置 Key 后事务可挂起数分钟，期间其他写者（手工 crawl 与 serve 定时任务并行时）锁等待 15s 后失败，中途崩溃整体回滚（含 raw_items 留痕）。因 max_instances=1 实际并发面小，评 P2
2. `db/database.py:58-65` — SCHEMA 删除了 `raw_items.processed` 列，但修复记录写“保留留档用途”，**记录与代码不一致**（旧库仍带该列，无代码引用，无功能影响）
3. `agent/classifier.py:38-45` — 修复记录称“classifier 大小写统一”，实际 diff 仅是 foreign 循环等价重构，central 匹配仍区分大小写，原 P2-20 的大小写不一致**实际未变**（对中文企业名无实害）
4. `crawler/sources/iguopin.py:41-46` — 单条容错只加给了 nowcoder；iguopin 的 `_to_item` 循环仍无单条 try，单条类型异常仍会毁 iguopin 整源本轮（被 pipeline 按源兜住）。修复不对称，非新增回归
5. `crawler/sources/nowcoder.py:91-99` — 公司页仍用裸 `httpx.get`，无重试（BaseSource.fetch 的重试/限速只覆盖到 POST 搜索路径），P2-8 修复不完整
6. `tests/test_dedup.py:46` — P2-23 未修：无 openai SDK 的环境跑不了“不依赖 Key”的测试（本轮实测复现）
7. `db/repository.py:7-8`、`crawler/sources/iguopin.py:4` — `JOB_COLUMNS` 死常量、iguopin 的 `from config import settings` 死导入
8. `app/templates/index.html:64,66` + `app/main.py:23` — 分页链接中 category/recruit_type 未 urlencode（构造 URL 可破坏分页链接，autoescape 仍防 XSS）；page 无下界校验（page=0 经实测 SQLite 负 OFFSET 视为 0，仅“第 0 页”观感问题）
9. 说明：现有 data/job.db 中修复前的历史行 last_seen 为 UTC、之后为北京时间，过渡期混合口径随 touch_job 刷新自愈

另：工作区有一份**未提交的 README.md 改动**（实测指标/已知边界），内容与修复一致，建议随修复一并提交。

## 五、总体结论

**可交付** — P1 六项全部真实修复到位（分页口径、Agent 接线、分类守卫+缓存、负缓存、字段兜底、日志路径均经代码审读 + sqlite3 行为实测 + 数据库实证确认），未发现本轮修复新引入的 P1 问题；py_compile 全过；上列遗留均为 P2 且已分级记录，可在后续迭代处理。

（一行结论：**可交付**）